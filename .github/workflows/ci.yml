name: CI

on:
  pull_request:
  push:
    branches: [master]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Biome check
        run: pnpm check

      # Runs the full vitest suite, which includes the axe-based
      # accessibility smoke at src/entrypoints/*/__tests__/a11y.test.tsx.
      # CI fails only on serious or critical violations — see
      # docs/ACCESSIBILITY.md for the policy.
      - name: Test
        run: pnpm test

      - name: Build
        run: pnpm build

      - name: Zip (Chrome)
        run: pnpm zip

      - name: Zip (Firefox)
        run: pnpm zip:firefox

      - name: Upload Chrome zip
        if: github.event_name == 'push' && github.ref == 'refs/heads/master'
        uses: actions/upload-artifact@v4
        with:
          name: better-bookmarks-chrome
          path: .output/*-chrome.zip
          if-no-files-found: error

      - name: Upload Firefox zip
        if: github.event_name == 'push' && github.ref == 'refs/heads/master'
        uses: actions/upload-artifact@v4
        with:
          name: better-bookmarks-firefox
          path: .output/*-firefox.zip
          if-no-files-found: error
